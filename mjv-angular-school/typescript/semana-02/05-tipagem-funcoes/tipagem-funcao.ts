type ArithimeticFunction = (x: number, y: number) => number;

const sum: ArithimeticFunction = (x, y) => x + y;

const multiply: ArithimeticFunction = (x, y) => x * y;

const subtraction: ArithimeticFunction = (x, y) => x - y;

const division: ArithimeticFunction = (x, y) => x / y;